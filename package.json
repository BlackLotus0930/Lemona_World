{
  "name": "lemona-game",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "bridge:dev": "npm --prefix bridge run dev",
    "demo:mock": "npm run dev",
    "demo:openclaw": "npm run dev -- --host"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.98.0",
    "pixi.js": "^8.16.0"
  },
  "devDependencies": {
    "typescript": "~5.6.2",
    "vite": "^6.0.1"
  }
}
